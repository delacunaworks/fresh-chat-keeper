/**
 * `@fresh-chat-keeper/judgment-engine` user-stats バレル。
 *
 * Phase 3.5（v0.5.0）視聴者フラグ機能の純粋層。chrome.storage 連携・DOM
 * 介入・UI は chrome-ext 側（B3 以降）で扱う。
 */
export * from './types.js';
export * from './flag-evaluator.js';
